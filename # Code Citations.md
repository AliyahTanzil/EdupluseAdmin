---
noteId: "105d2f80199511f1a42219820e556c8f"
tags: []

---

# Code Citations

## License: unknown
https://github.com/adishan123/freshKTM/blob/50275e2880f4fbb6e1e6c73cf011587b2524cbcc/src/component/table/Table.jsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```


## License: MIT
https://github.com/tanzeelrana/crossover/blob/a46fbbd1627436935ef5d0bb675c4574920c70d6/src/components/Table.tsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```


## License: unknown
https://github.com/grvv/sppt-task/blob/411aee2bdea6c64002bf72a9b8e28c8b547be5f2/src/components/Table.tsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```


## License: MIT
https://github.com/WTFAcademy/solui/blob/807999dec84bdb41d4d8c866e0fdb9cba1ef21c8/src/components/Table.tsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```


## License: unknown
https://github.com/holabayor/contact_manager/blob/00d1cf3c1b2b5c35e45873c9ccea6f33fd60c596/frontend/components/Table.tsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```


## License: unknown
https://github.com/Eric-Amaya/FrontEnd_Minera/blob/018473bdb4eebd6658e89c5d9356c60de7e6dde8/src/app/adm/reportes/components/Tabla.jsx

```
-gray-50">
          <tr>
            {columns.map((column, index) => (
              <th key={index} className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                {column.header}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="bg-white divide-y divide-gray-200">
          {data.map((row, rowIndex) => (
            <tr key={rowIndex} className="hover:bg-gray
```

